package app.banafsh.android.ui.theme

enum class ColorSource {
    Default,
    Dynamic,
    MaterialYou,
}

enum class ColorMode {
    System,
    Light,
    Dark,
}

enum class Darkness {
    Normal,
    AMOLED,
    PureBlack,
}
